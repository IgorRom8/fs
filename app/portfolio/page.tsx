import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link";
import { getPublishedProjects } from "@/src/entities/project/api/projects-repository"; import { PageHero } from "@/src/shared/ui/page-hero"; import { PageShell } from "@/src/widgets/page-shell";
export const metadata: Metadata = { title: "Портфолио | Фасадная симфония", description: "Фасадные проекты компании в Москве." };
export default async function PortfolioPage() { const projects=await getPublishedProjects(); return <PageShell><PageHero eyebrow="Портфолио" title="Фасады, которые формируют город" image="/portfolio/sudakova-1.webp" description="Избранные жилые, городские и инфраструктурные проекты." /><section className="listing content-section">{projects.map((project,index)=><Link className="listing-card project-listing" href={`/portfolio/${project.slug}`} key={project.slug}><div className="listing-image"><Image src={project.cover} alt={project.title} fill sizes="(max-width: 800px) 100vw, 50vw" unoptimized={project.cover.startsWith("/api/")} /></div><div className="listing-copy"><span>0{index+1} · {project.category} · {project.status}</span><h2>{project.title}</h2><p>{project.location}</p><i>Смотреть проект</i></div></Link>)}</section></PageShell>; }
