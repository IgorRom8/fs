import Link from "next/link"; import { requireAdmin } from "@/src/shared/lib/admin"; import { FileDropzone } from "@/src/shared/ui/file-dropzone"; import { createNews } from "../../actions";
export default async function NewNews(){await requireAdmin();return <main className="admin-editor"><Link href="/admin">← Назад</Link><h1>Новая новость</h1><form action={createNews}><label>Заголовок<input name="title" required/></label><label>Адрес страницы <small>Необязательно — сформируется из заголовка</small><input name="slug" maxLength={100} placeholder="Например: Новый объект в Москве"/></label><label>Краткое описание<textarea name="excerpt" required rows={3}/></label><label>Текст новости<textarea name="content" required rows={12} placeholder="Разделяйте абзацы пустой строкой"/></label><FileDropzone name="cover" label="Обложка" required/><label className="admin-check"><input name="published" type="checkbox"/>Опубликовать сразу</label><button type="submit">Сохранить новость</button></form></main>}
