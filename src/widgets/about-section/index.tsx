"use client";
import { useScrollReveal } from "@/src/shared/lib/use-scroll-reveal";
import { Arrow } from "@/src/shared/ui/arrow";
import { SectionLabel } from "@/src/shared/ui/section-label";
export function AboutSection({ daysOnMarket, totalArea }: { daysOnMarket: number; totalArea: number }) { useScrollReveal(); const stats = [[new Intl.NumberFormat("ru-RU").format(daysOnMarket), "дней на рынке"], [new Intl.NumberFormat("ru-RU",{maximumFractionDigits:2}).format(totalArea), "м² фасадов"], ["100%", "собственный штат"], ["5", "лет гарантии"]]; return <section className="intro section" id="about"><SectionLabel index="01">О компании</SectionLabel><div className="intro-copy" data-reveal><h2><span>Забираем на себя весь фасад,</span><em>оберегая ваше спокойствие</em></h2><div className="intro-text"><p>Мы не просто подрядчик, а сплочённая команда инженеров и монтажников. Берём ограниченное число объектов, чтобы вложить максимум внимания в каждый.</p><a className="text-link" href="#services">Узнать больше <Arrow /></a></div></div><div className="stats" data-reveal>{stats.map(([value, label]) => <article key={label}><strong>{value}</strong><span>{label}</span></article>)}</div></section>; }
